ANTHROPIC_API_KEY=your-api-key-here

# Comma-separated origins allowed to call the dev API server (server/index.ts).
# Defaults to the Vite dev (5173) and preview (4173) origins.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173
